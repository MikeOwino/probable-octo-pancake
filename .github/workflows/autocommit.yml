name: Auto commit

on:

  push:
    branches:
      - master
      
  schedule:
  - cron: "*/10 * * * *"

jobs:
  auto_commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2      
        with:
         persist-credentials: false
         fetch-depth: 0

      - name: Modify last update
        run: |
          d=`date '+%Y-%m-%dT%H:%M:%SZ'`
          echo $d > LAST_UPDATED
          
      - name: Commit changes
        run: |
          git config --local user.email "mike@mikeowino.com"
          git config --local user.name "MikeOwino"
          git add -A
          
          arr[0]="probable-octo-pancake"
          arr[1]="probable-octo-pancake"
          arr[2]="probable-octo-pancake"
          arr[3]="probable-octo-pancake"
          arr[4]="probable-octo-pancake"
          arr[5]="probable-octo-pancake"
          arr[6]="probable-octo-pancake"
          arr[7]="probable-octo-pancake"
          
          rand=$[$RANDOM % ${#arr[@]}]
          
          git commit -m "${arr[$rand]}"
          
      - name: GitHub Push
        uses: ad-m/github-push-action@v0.5.0
        with:
          force: true
          directory: "."
          github_token: ${{ secrets.GITHUB_TOKEN }}
